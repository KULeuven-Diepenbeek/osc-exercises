#include <stdio.h>
#include <stdlib.h>


// TODO define Orc struct


Orc* generate_orcs(int amount) {
  Orc* army = malloc(sizeof(Orc) * amount);
  // add stuff to army
  return army; 
}

int main() {
	// TODO
	return 0;
}